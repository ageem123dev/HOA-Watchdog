# generated: 2026-07-30
# last_updated: "2026-08-10"
# project: HOA-Treasurer-Assistant
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones
#
# NOTE: Epics 2, 3 and 4 are named in the epic list but have no story breakdown yet
# (_bmad-output/planning-artifacts/epics.md details Epic 1 only). Re-run sprint planning
# after their stories are written to populate them here.

generated: 2026-07-30
last_updated: 2026-08-19
project: HOA-Treasurer-Assistant
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  epic-1: done
  1-1-project-scaffold-with-a-verified-build: done
  1-2-board-member-sign-in: done
  1-3-visual-foundation: done
  1-4-upload-a-document-and-see-it-accepted-or-rejected: done
  1-5-read-a-document-into-structured-records: done
  1-5b-store-extracted-records-and-complete-ingestion: done
  1-5c-extract-structured-records-from-pdfs-and-images: done
  1-5d-extract-on-upload-and-show-progress: done
  # Epic story 1.6 is delivered as four stories; see epics.md for the AC mapping.
  # Order matters: 1.6d resolves from the queue 1.6c builds.
  1-6a-recognise-known-vendors: done
  1-6b-hold-unknown-vendors-for-a-human: done
  1-6c-see-what-is-waiting: done
  1-6d-resolve-a-held-document: done
  epic-1-retrospective: done
  epic-2: done
  2-1-units-and-who-holds-them: done
  2-2-what-each-unit-owes-this-year: done
  2-3-what-is-due-and-by-when: done
  2-4-deposits-become-payments: done
  2-5-deposits-become-payments-on-upload: done
  # Closes the epic. Re-baseline onto 2.5's merge commit before starting; it
  # documents the payment-on-upload path 2.5 builds.
  2-6-documentation-and-sample-uploads: done
  # Removes the trap 2.6 documents: nothing creates a unit, so every deposit on a
  # real installation is held unknown-unit. Better run BEFORE 2.6, which then
  # documents a working roll instead of warning about a missing one.
  2-7-the-roll-becomes-units-and-assessments: done
  epic-2-retrospective: optional

  epic-3: done
  # AD-12 and AD-14. Provenance lands with the first execution path or becomes a
  # retrofit that logs only some paths.
  3-1-the-catalog-executed-and-logged: done
  3-2-tool-endpoints-as-the-only-way-in: done
  # Introduces Python. MUST add pytest to the local gate in this story -- a
  # package.json script AND the "Tested =" line in bmad-ship-story. There is no
  # CI, so forgetting it ships a second language with no automated check at all.
  3-3-the-python-service-exists: done
  3-4-the-model-picks-an-entry: done
  # AD-7. Must exist BEFORE the first answer renders (3.6), or that story ships
  # precisely the failure this product exists to prevent.
  3-5-the-numeric-validator: done
  # Split 2026-08-11. Story 3.6 as written meant a Python HTTP server, a Node
  # client for it, and three UX requirements — more than 1.5d, which drew five
  # review rounds at 27 files. 3.6a proves a question round-trips; 3.6b builds
  # the surface on it. Blocked until AD-17 (the Node->agent wire) is approved.
  3-6a-the-chat-turn-crosses-the-wire: done
  3-6b-ask-and-answer: done
  3-6c-the-dashboard-ask-field: done
  3-7-when-it-cannot-answer: done
  3-8-the-access-log: done
  epic-3-retrospective: done

  epic-4: done
  4-1-a-finding-and-the-life-it-leads: done
  4-2-the-same-invoice-twice: done
  4-3-a-vendor-who-charged-more-than-usual: done
  4-4-the-dues-that-did-not-arrive: done
  4-5-the-board-sees-what-needs-review: done
  4-6-one-finding-and-what-to-do-about-it: done
  4-7-the-register-the-board-hands-an-auditor: done
  4-8-told-before-you-pay: done
  epic-4-retrospective: done

  # Epic 5 is ONBOARDING as of 2026-08-18. Connected document sources moved to
  # epic 6 -- adding a second intake channel before the first can be used by a
  # stranger was the wrong order.
  epic-5: in-progress
  5-1-the-association-exists: review  # the schema retrofit and the write path
  5-1b-the-catalog-answers-for-one-association: backlog  # split from 5.1: the read path
  5-2-a-document-declares-its-kind: backlog  # kind moves from the row to the upload; contract change
  5-3-the-headers-we-were-given: backlog  # duplicates and blanks reported, not guessed
  5-4-mapping-one-column-to-another: backlog  # drag AND keyboard, neither the poor relation
  5-5-what-this-mapping-would-produce: backlog  # the preview, before anything is stored
  5-6-a-guess-offered-not-applied: backlog  # FR-10; deterministic first, model on the residue
  5-7-the-mapping-is-remembered: backlog  # and a mapping change is a deliberate act
  5-8-the-order-that-works: backlog  # no deposits before a roll
  5-9-the-first-directors: backlog  # retires or constrains scripts/add-board-member.mjs
  epic-5-retrospective: optional
  epic-6: backlog
  epic-6-retrospective: optional

action_items:
  - epic: 5
    action: "Retire or constrain scripts/add-board-member.mjs once story 5.9 lands a product provisioning flow. It writes board_member directly with WATCHDOG_WRITER_DATABASE_URL, so until then provisioning is permanently reachable by anyone holding a database URL - and FR-9 claims directors are provisioned through the product. Raised by CodeRabbit on MR !67"
    owner: "Dev"
    status: open
  - epic: 5
    action: "Story 5.6 puts a reasoning model in the intake path, where AD-8 forbids interpolating extracted strings into a prompt - and column headers are extracted strings. The mitigation is AD-8's own shape rather than a sanitiser: the model proposes, a human confirms, nothing is created automatically. Assert the refusal, not just the suggestion. Also state where the line sits relative to epic 4's no-model-in-alerts guard, which covers FR-6/7/8 and not intake, so the deterministic claim is not read as quietly weakened"
    owner: "Dev"
    status: open
  - epic: 5
    action: "Build 5.6's deterministic matcher before the model call. Case, punctuation and abbreviation cover most real headers - Txn Date, Descr, Amt, Unit # - at no cost, no prompt and no credential. The model earns the residue, and building it that way is what makes FR-10's requirement to stay usable when the model is unreachable true by construction rather than by a fallback nobody tests"
    owner: "Dev"
    status: open
  - epic: 5
    action: "PREREQUISITE, before any epic 5 story: write the FR. The PRD stops at FR-8 and none of them cover onboarding, import configuration or provisioning, so the epic currently cites nothing. Every previous epic cites an FR; this would be the first that does not"
    owner: "Matt"
    status: done  # FR-9 and FR-10 written 2026-08-18
  - epic: 5
    action: "PREREQUISITE, before story 5.1: amend AD-4 and AD-5 for the association entity. The architecture defers multi-tenancy explicitly and names both as needing revision before a second association is onboarded. Introducing association_id across every table that holds association data is a retrofit, not a schema tweak, and it is the expensive half of epic 5 rather than the wizard"
    owner: "Matt"
    status: open
  - epic: 5
    action: "Decide at story 5.6 what a CHANGED mapping does to documents already imported. AD-13's content-hash idempotency covers re-ingesting the same bytes; it says nothing about the same bytes parsed under a different mapping, because nothing could change how they parsed before. Re-import what it affects, or apply only to what arrives next - and say which where a treasurer can read it"
    owner: "Dev"
    status: open
  - epic: 4
    action: "Add `* text=auto` to .gitattributes and run `git add --renormalize .`. The file exists but covers only samples/* binaries, so every source file is still index-LF / worktree-CRLF with no attribute declaring it, and every commit in epics 3 and 4 emitted CRLF warnings. Not cosmetic: scripted edits repeatedly failed to match anchors written with \n, and on story 4.8 one such failure silently did not apply and was then reported as fixed on a review thread. CodeRabbit's round-2 re-review is what caught it"
    owner: "Dev"
    status: open
  - epic: 4
    action: "Scope migrations/roles.test.ts's unbounded update and delete by RUN_PREFIX and replace the TRUNCATE probe with an information_schema assertion - the exact change already made in query-log.test.ts. THIRD epic of asking; it was epic 3's highest-consequence open item and was carried untouched through epic 4. On the one run where the reader grant has regressed, this test is the thing that rewrites or empties board_member"
    owner: "Dev"
    status: open
  - epic: 4
    action: "Extend the test-value pass in _bmad/custom/review-gate.md 2a: for EVERY assertion added or changed in a round, name the mutation that would fail it - including FIXTURE mutations, not only production ones. Story 4.8 shipped five tests that proved nothing and the sensitivity check caught one; the four that escaped were fixture-vacuous (an amount containing the substring asserted, two reads of an unchanged table, an assertion restating its neighbour), which mutating production code cannot surface. Third epic this class has appeared"
    owner: "Dev"
    status: open
  - epic: 4
    action: "Migrate the last two declaredMembers copies (assessment-directory.test.ts, roll-repository.test.ts) to core/ports/declared-members.ts"
    owner: "Dev"
    status: open
  - epic: 4
    action: "DECISION NEEDED before epic 5 can be planned: epic 5 (connected document sources) has no story breakdown and carries a blocking prerequisite - AD-1 says data enters exclusively through user upload, which epic 5 makes false, and the spine requires the amendment to land BEFORE the epic starts. Separately: epics 1-4 are complete and the product has never been run by a board member, so epic 5 adds a second intake channel to a first one that has never been used in anger. Amend AD-1, deploy and pilot what exists, or re-sequence"
    owner: "Matt"
    status: done  # RESOLVED 2026-08-18: onboarding becomes epic 5, connected sources epic 6. The product has now been exercised (Oracle + dashboard), and intake is the gap
  - epic: 4
    action: "Commit or discard the three tooling files carried uncommitted across two epics: .gitignore (.argus/ entry), .mcp.json (Argus path), and .claude/commands/argus-review.md - the last of which carries the known $ARGUMENTS shell injection that is itself an open action item"
    owner: "Matt"
    status: open
  - epic: 4
    action: "docs/no-control-characters.test.ts reads markdown only, and the defect it exists for landed again on story 4.6 in a .ts file - a word-boundary escape written through a shell heredoc became a literal backspace byte inside a regex, which then compiled fine and matched nothing. That is the same failure its own header records from story 3.5, in the same position. A scan of all 396 source files found forbidden control bytes in exactly two, both deliberate fixtures (core/extraction/tabular-roll.test.ts and the guard's own file), so widening the glob to .ts/.tsx needs only a two-entry allow-list - and the allow-list itself needs justifying so it does not become a dumping ground. Worth checking the YAML too: this entry originally carried the escape verbatim, and a double-quoted YAML scalar would have turned it into the same byte. Not done in 4.6: a repo-wide guard change beyond that story's ACs, raised at ready-to-merge"
    owner: "Dev"
    status: open
  - epic: 1
    action: "Restore CI after the heavy development cycle. Config is at 621d4cc:.gitlab-ci.yml; prefer a self-managed runner, which consumes no GitLab compute minutes. See AD-2 amendment"
    owner: "Matt"
    status: open
  - epic: 1
    action: "DECIDED 2026-08-07: CI removed entirely (GitLab per-minute billing). AD-2's CI assertion withdrawn; local gate is the only gate. Restore nfr2-guard first if budget returns"
    owner: "Matt"
    status: decided
  - epic: 1
    action: "First Python story adds pytest to the local gate (package.json script + the Tested= line) in the same story - there is no CI as of 2026-08-07"
    owner: "Dev"
    status: done
  - epic: 1
    action: "Fix $ARGUMENTS shell injection in .claude/commands/argus-review.md"
    owner: "Matt"
    status: open
  - epic: 1
    action: "Make .mcp.json and argus-review.md Argus paths portable (change together)"
    owner: "Matt"
    status: open
  - epic: 1
    action: "extraction.vendor_id, written from both the ingest and resolution paths in one change"
    owner: "Dev"
    status: deferred
  - epic: 1
    action: "Shared connection pool - seven module-scoped pools at max 5 against max_connections 100. Story 3.1 added the eighth and ninth (catalog-executor, query-log), and CodeRabbit raised the disposal half again there. Story 3.2 turned it from latent into real: widening test:db to app/tools/ put those two pools in parallel with the other 39 files and roll-ingestion.test.ts began timing out at 5s on roughly one run in three. Verified as caused by the widening - the pre-change scope passed 3/3, the widened one failed 1/3 - and mitigated by running app/tools/ as a second sequential vitest invocation. That is a workaround; the fix is this entry. Story 2.1 added the seventh (unit-directory) and a review raised the second half of the same defect: a module-scoped pool is orphaned on every Next.js HMR reload in dev, so connections leak until the limit is hit. Both halves want one change across all seven adapters, not one file at a time."
    owner: "Dev"
    status: open
  - epic: 2
    action: "The TypeScript comment stripper in the two *-connection.test.ts files strips // with a naive regex that is not quote-aware, so a // inside a string literal (a URL) would delete the rest of the line before the source assertions run. Latent: neither adapter contains one today. Raised by Argus on story 2.1; migrations/executable-sql.ts is the quote-aware equivalent for SQL if this ever needs writing."
    owner: "Dev"
    status: open
  - epic: 2
    action: "core/ports/unit-directory.test.ts declaredMethods matches only method-shorthand syntax (/^\\s*(\\w+)\\s*\\(/gm), so a write capability declared as a function-typed property (readonly record: (x) => Promise<void>) is invisible and the exhaustive read-only assertion still passes. Same hole was found and fixed in assessment-directory.test.ts on story 2.2; the fix is /^\\s*(?:readonly\\s+)?(\\w+)\\s*[:(]/gm."
    owner: "Dev"
    status: open
  - epic: 2
    action: "All six migrations/*.test.ts use __dirname while package.json is type: module; they rely on Vitest polyfilling it. The ports and adapters family uses import.meta.url. Pick one across the repo rather than per-file."
    owner: "Dev"
    status: open
  - epic: 2
    action: "adapters/db/unit-directory-connection.test.ts has a comment-stripping control that proves nothing: it asserts the absence of the opening line of its OWN docblock, a phrase that never appears in the adapter it reads, so it holds with the stripping deleted. Found on story 2.2 where the same shape had been copied. Fix: assert a single-line phrase present in the adapter before stripping and absent after."
    owner: "Dev"
    status: open
  - epic: 2
    action: "Both replace() paths insert one row per record in a for-of loop, holding a SELECT FOR UPDATE lock across hundreds of roundtrips for a CSV bank feed. Applies to adapters/db/extraction-repository-postgres.ts (epic 1) and adapters/db/payment-repository-postgres.ts (story 2.4) equally. Raised twice by Argus. Fix both together with UNNEST or a multi-row INSERT rather than diverging one from the other."
    owner: "Dev"
    status: open
  - epic: 1
    action: "Add .gitattributes and renormalise the repo-wide CRLF"
    owner: "Dev"
    status: open
  - epic: 1
    action: "Tests for readWriterDatabaseUrl and MissingAuthConfigError, which have none"
    owner: "Dev"
    status: open
  - epic: 1
    action: "Migration 006 padded-ends btrim bypass, which migration 009 fixed and 006 did not"
    owner: "Dev"
    status: open
  - epic: 1
    action: "DECIDED 2026-08-07: no tsc --noEmit CI gate; kept a local habit. Revisit if type errors reach main"
    owner: "Matt"
    status: decided
  - epic: 3
    action: "migrations/roles.test.ts runs unbounded destructive statements to prove the reader cannot write - `update board_member set display_name = 'changed'` and `delete from board_member` with no WHERE. On the run where the grant has regressed, the test that exists to catch it is the thing that rewrites or wipes the table. Story 3.1 hit the identical shape in migrations/query-log.test.ts and scoped both statements by RUN_PREFIX; Postgres checks the privilege before matching rows, so the assertion is unchanged. TRUNCATE has no scoped form and was replaced there by an exact-set assertion over information_schema. Same fix applies here."
    owner: "Dev"
    status: open
  - epic: 3
    action: "The declaredMembers helper is copied into five core/ports/*.test.ts files (unit-directory, assessment-directory, quarantine-queue, roll-repository, vendor-resolution). Story 3.1 extracted a shared core/ports/declared-members.ts and used it for the new port test only. All five copies carry a bug the shared one now fixes: the interface is found with indexOf, so a lookup for `Foo` matches `FooEntry` and the test silently reads a neighbouring interface's body. Migrate the five to the shared helper in one sweep. EPIC 4 RETRO: three migrated and every new epic-4 port test used the shared helper; assessment-directory.test.ts and roll-repository.test.ts still carry local copies."
    owner: "Dev"
    status: in-progress
  - epic: 3
    action: "AD-14's freeze is anchored in-repo: catalog/published-versions.json is read from the same checkout as the entry it pins, so one commit can change both and the tests still pass. Raised by CodeRabbit on MR !35, which proposed protected GitLab releases or a signed external manifest consumed by CI. Declined for story 3.1 - the mechanism needs CI, removed 2026-08-07 for cost, and externalising the anchor is a new AD rather than a story fix. The pilot's threat model is an honest team plus review, and the committed digest is what puts a change in the diff. Revisit with the CI action item."
    owner: "Matt"
    status: open
  - epic: 3
    action: "ARCHITECTURE DECISION for Matt: AD-3 and AD-15 contradict each other on how many secrets the agent service holds. AD-3 says 'exactly one secret - the model API key'; AD-15's mechanism, decided later on 2026-07-31 and marked previously-deferred, requires 'a shared service token held only by the agent service and the gateway'. Story 3.3 implements both - the token is required and no data credential is permitted - but the spine's wording is self-contradictory. Defensible fix: amend AD-3 to 'one model credential and the gateway service token, and never a data credential'. Raised by CodeRabbit on MR !39 and left unresolved because amending an invariant is not a story's to make."
    owner: "Matt"
    status: done
  - epic: 3
    action: "Shared connection pool - one change across all ten module-scoped adapters. DECIDED at the epic 3 retro: do this BEFORE epic 4. Three epics carried, one real test:db flake now mitigated by a load-bearing workaround, and Argus raised the disposal half again on story 3.8"
    owner: "Dev"
    status: done  # 14 pools (not 10) consolidated to 2; test:db back to one invocation. CONFIRMED still holding at the epic 4 retro
  - epic: 3
    action: "migrations/roles.test.ts proves the reader cannot write by running unbounded update and delete with no WHERE. On the one run where the grant has regressed, the test wipes the table it is checking. Story 3.1 fixed the identical shape in query-log.test.ts by scoping to RUN_PREFIX - Postgres checks the privilege before matching rows, so the assertion is unchanged. Rank above the rest of the backlog"
    owner: "Dev"
    status: open
  - epic: 3
    action: "Add .gitattributes and run git add --renormalize . - every commit in epic 3 emitted CRLF warnings"
    owner: "Dev"
    status: open
  - epic: 3
    action: "Settle epic 4's open question - detection and alert copy deterministic vs model-generated - BEFORE story 4.1. The epic's standalone claim is conditioned on it"
    owner: "Matt"
    status: done  # CONFIRMED 2026-08-12 by Matt: deterministic. SQL detects, templated prose describes, no model in FR-6/7/8. Epic 4 stays independent of epic 3 and SM-2 stays falsifiable
  - epic: 3
    action: "Epic 3 is complete and entirely unexercised: never deployed, no board member has seen it, and the catalog holds one entry. Every claim of correctness is about tests and reviews, not about a treasurer asking a question"
    owner: "Matt"
    status: open
