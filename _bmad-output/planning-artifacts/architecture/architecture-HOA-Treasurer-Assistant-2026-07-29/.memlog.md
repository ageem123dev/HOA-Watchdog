---
scope: AI Condo Treasury Bot (Fiduciary Watchdog) — early-adopter pilot: document ingestion, Conversational Oracle, passive anomaly detection
purpose: build-substrate
altitude: feature
updated: 2026-07-30T11:01
---

- (event by coach) Run started: create intent, Coaching path. Sole input is docs/prd/prd.md (PRD status Final, pilot scope). No UX spec, no epics, no project-context.md, greenfield repo (no package.json, no source yet).
- (direction by user) Deliverables: the spine, plus three renderings at Finalize — (1) board-facing non-technical explainer of the air-gap, (2) security/compliance write-up covering dual-LLM, read-only roles, query provenance as HB 1021 diligence evidence, (3) interactive HTML+SVG walkthrough deck.
- (constraint by coach) Environment: uv not installed; python3 3.14.6 available — all BMad scripts run via python3.
- (decision by user) Uploads-only data plane: no connection to any external accounting system or bank. All ledger-like data (deposits, assessment roll, invoices) arrives via uploaded CSV/PDF/image. The Watchdog owns its own read-write store. Binds: FR-1, FR-7, NFR-1, NFR-2. Prevents: builders assuming a live ledger integration exists, or disagreeing on which store is authoritative.
- (decision by user) 'Air-gap' is redefined structurally as the ABSENCE of any outbound write path — no bank/QuickBooks credentials exist in any environment, so there is no rail to write to. It is not a permission setting that could be widened later. Prevents: a future story 'just adding' a write integration without an explicit architecture change.
- (decision by user) Two-runtime split CONFIRMED (not merely inherited): Next.js/Node gateway owns UI, auth, uploads; a Python service owns agent orchestration and deterministic tooling. Cost accepted: two deploy units, two test harnesses, an explicit wire contract.
- (question by coach) OPEN — PRD NFR-1 says the Node gateway and Python service authenticate to 'the database' with read-only roles. Under the uploads-only decision there is no external DB, so NFR-1 as written is unsatisfiable/meaningless. Proposed reinterpretation preserving its intent: role separation by PIPELINE STAGE inside our own store — the ingestion writer role may INSERT, the Oracle query role is SELECT-only — so the LLM-driven path structurally cannot mutate. Needs user ratification + a PRD update.
- (decision by user) Oracle query surface = PARAMETERIZED QUERY CATALOG. The reasoning model never authors SQL; it selects a named catalog entry and supplies typed params. Binds FR-4, FR-5, NFR-5. Prevents: unreviewed SQL reaching member financial data, and prompt-injected query generation. Cost accepted: a new question shape is a new catalog entry (a story), not a runtime capability.
- (decision by user) Zero-LLM Token Arithmetic enforced by PROVENANCE-BOUND VALIDATION, not prompt directives. Every numeric token in a rendered answer must match a value present in the tool result set; a pre-render validator rejects unreferenced numerals and forces a retry. Makes SM-1 a real assertion. Supersedes NFR-3's prompt-only mechanism.
- (constraint by coach) COROLLARY of the two decisions above: catalog queries MUST return every number the answer needs, INCLUDING derived values (deltas, percentages, trailing averages, counts). The model cannot compute them and the validator would reject them. FR-6's '20% over trailing 6-month average' is therefore a catalog query returning the computed percentage, not a raw average the model reasons over. Numeric normalization (1240 vs $1,240.00) needs an explicit comparison rule in the validator.
- (constraint by coach) FR-5 literal wording ('view the exact SQL query or Python snippet executed') is still satisfied by the catalog: the UI shows the reviewed SQL text of the chosen entry plus the bound parameter values.
- (decision by user) Quarantine boundary hardened beyond FR-2/FR-3: extracted field VALUES get value-level constraints (length caps, format regex, enums; vendor names resolved to a known-vendor table with unknowns routed to a human-confirm quarantine queue) AND extracted strings are NEVER string-interpolated into any prompt. Prompts carry row ids; tools resolve values; the renderer escapes on output. Prevents: a schema-valid injection payload in a field value steering the anomaly agent or reaching board email.
- (decision by user) Credential topology: Node holds ALL database credentials — a writer role for ingestion and a SELECT-only role for catalog execution. The Python agent service holds NO database credentials at any time; it orchestrates and calls back to Node's tool endpoints for every fact. Prevents: the LLM-adjacent runtime ever holding a write path. Accepts a callback round-trip per tool call (SM-C1 explicitly trades latency for accuracy).
- (question by coach) OPEN — Node↔Python service-to-service authentication is now load-bearing: Node's /tools/* endpoints are the only data path and must not be callable by anything but the agent service. Mechanism undecided (mTLS / signed service token / private network only).
- (question by coach) OPEN — CHALLENGE TO RE-OPEN: the two-runtime split was confirmed BEFORE the credential decision moved deterministic tooling into Node. The Python service is now orchestration-only (LLM key, no DB, no tools). Does a second runtime still earn two deploy units and two test harnesses? Case for: real process isolation for LLM-adjacent code, CrewAI ecosystem. Case against: TS agent SDKs are mature, the original justification (deterministic math in Python) has moved out.
- (decision by user) Two-runtime split RE-RATIFIED after explicit challenge: the separate Python process IS the dual-LLM boundary made physical — LLM-adjacent code runs in its own container holding exactly one secret (the model API key) and no data path. This is the claim the board explainer and compliance write-up rest on. Two deploy units and two test harnesses (Vitest + pytest) accepted knowingly. CrewAI retained. Closes the re-open challenge.
- (decision by user) Deployment target: Next.js on Vercel; Postgres + auth + object storage on Supabase; Python agent service on a container host. Supabase's role/grant model is what the Node-holds-all-credentials decision depends on. Consequence to carry into the compliance write-up: association financial data resides on third-party infrastructure.
- (question by coach) OPEN — extraction service for FR-2 is still undecided (Google Document AI / AWS Textract / Azure Document Intelligence / a VLM). It is the untrusted half of the dual-LLM pattern, so its choice is load-bearing, not seed. Needs web verification before binding.
- (version by coach) Web-verified 2026-07-29: Next.js 16.2.12 stable (16.2.11 Active LTS, released 2026-07-25); CrewAI 1.15.8 latest on PyPI.
- (constraint by coach) CONFLICT — CrewAI's PyPI requires_python is '<3.14,>=3.10'. This machine runs Python 3.14.6, so CrewAI cannot install on the ambient interpreter. The agent service must pin Python 3.13 in its container/venv. Consequence for the dev pipelines: the pytest harness for the Python service runs on 3.13, not the system 3.14.
- (version by coach) Current Anthropic model line (verified via claude-api skill, cached 2026-06-24): Opus 5 (claude-opus-5, 1M ctx, $5/$25), Sonnet 5 (claude-sonnet-5), Haiku 4.5 (claude-haiku-4-5), Fable 5. PRD NFR-4's 'Claude 3.5 Sonnet' pin is several generations stale; 3.5 Sonnet is retired (Oct 2025) and returns 404.
- (constraint by coach) STRUCTURAL FIT for the catalog decision: Anthropic strict tool use (top-level 'strict: true' on a tool definition, requires additionalProperties:false + required) GUARANTEES tool_use.input validates exactly against the schema. That makes the parameterized query catalog enforceable at the API layer, not just by prompt — the model literally cannot emit malformed params. Supported on Opus 5, Opus 4.8, Sonnet 5, Haiku 4.5.
- (decision by user) Extraction (FR-2) = Gemini 3.1 Flash-Lite (model id 'gemini-3.1-flash-lite'; the -preview variant is shut down). Chosen from the user's direct production experience: works very well at very low cost for this parsing. Schema conformance (FR-3) is enforced at the API layer via responseMimeType=application/json + responseSchema. Verified 2026-07-29.
- (decision by coach) EMERGENT PROPERTY of the Gemini-extractor + Claude-reasoner split: the dual-LLM boundary is now a VENDOR boundary as well as a context boundary. The extractor and the reasoning agent share no credentials, no context, and no control plane. Strengthens the FR-2 isolation claim beyond what a single-vendor design could assert — carry this into the compliance write-up.
- (decision by user) Reasoning/tool-calling model = claude-sonnet-5 throughout (Oracle and anomaly narration). User chose Sonnet 5 over the coach's Opus 5 recommendation after the fiduciary-risk counter-argument was stated. Capability bar still satisfied: Sonnet 5 supports strict tool use and schema-validated structured outputs, which is what the catalog invariant actually requires. Supersedes PRD NFR-4's retired 'Claude 3.5 Sonnet' pin.
- (constraint by coach) Newer Gemini Flash tiers exist (3.5 Flash-Lite, 3.6 Flash) but 3.1 Flash-Lite is bound on the user's measured cost/quality experience. Treat the model id as replaceable seed, not an invariant — the invariant is API-layer schema enforcement.
- (event by coach) Coaching complete: 10 decisions, 2 open questions remaining (NFR-1 PRD update, Node-Python service auth mechanism). Proceeding to distillation.
- (event by coach) Reviewer gate run INLINE (not via parallel subagents — user has a standing instruction against spawning agents unless requested). lint_spine.py: 0 findings. Web-verification lens: satisfied (Next.js, CrewAI, Gemini model id, Anthropic model line all verified against live sources this session).
- (decision by coach) AD-13 added from the adversarial lens: ingestion idempotency on document content hash. Hole found — two ingestion paths (first upload, re-process, retry) could each obey every prior AD and still write duplicate invoice/payment/alert rows. In a duplicate-invoice-detection product that is a self-inflicted false positive. Alerts keyed on (finding_type, subject_id, period).
- (decision by coach) AD-14 added from the adversarial lens: catalog entry versions are immutable once used in production. Hole found — AD-12 logs (entry_id, version) but nothing forbade editing a version's SQL in place, which would make the audit trail unreplayable. Also fixes single-owner rule for vendor identity creation.
- (event by coach) Prior AD-13 (two runtimes / wire contract) renumbered to AD-15 during insertion. IDs are stable from this point forward.
- (decision by user) AIR-GAP SCOPE CORRECTED by the user: the air-gap means the system does not update BANKING information directly. It CAN modify its own data sources, including its own database. The PRD's 'strictly a read-only intelligence layer' wording overstated the constraint and is wrong. AD-2 amended in place to say so explicitly; internal write capability is partitioned by AD-4, not withheld.
- (event by coach) PRD updated at docs/prd/prd.md to remove all divergence from the spine: Air-Gap glossary rewritten; non-goal 'Ledger Mutation' renamed 'External Ledger Mutation' and scoped to external systems of record; NFR-1 rewritten as role separation by pipeline stage; NFR-1a added (no data credentials in the LLM runtime); NFR-2 sharpened to absence-of-credential; NFR-3 rewritten to provenance-bound validation; NFR-4 rewritten as a capability bar with claude-sonnet-5 as the current binding.
- (event by coach) Three renderings built and published as artifacts: board-explainer.html (non-technical, for the board), security-posture.html (control register with typed controls + explicit non-coverage), architecture-walkthrough.html (11-slide keyboard-navigable deck). Shared visual identity grounded in accounting-ledger vernacular: monospace as display voice, Charter/Sitka serif body, ledger-green accent with oxide red reserved for semantic flag use.
- (event by coach) spine finalized — status: final, updated 2026-07-30. Upstream reconciliation complete; the Open Questions section was replaced by an Upstream Reconciliation table since all PRD divergence is now resolved at the source.
- (direction by coach) NEXT: bmad-create-epics-and-stories off the PRD + this spine, then bmad-sprint-planning, then the ship/epic pipelines (which now run bmad-dev-tdd test-first).
- (decision by user) CLOUD STORAGE INGESTION requested 2026-07-30 (Dropbox / Google Drive). Decision: add as a LATER epic; direct upload remains the primary path and is not replaced. Rationale for sequencing: keeps an OAuth lifecycle off the critical path to first value, and the connector then builds on an ingestion pipeline already proven and already idempotent.
- (decision by user) Connector to be built as a PROVIDER-AGNOSTIC PORT with two adapters (Dropbox + Google Drive), not one provider hard-wired. Accepts more upfront work to avoid the rewrite when the second provider is requested.
- (constraint by coach) Verified 2026-07-30: Google drive.readonly is a RESTRICTED scope requiring a third-party CASA security assessment (~$500-$4,500/yr by tier) with mandatory re-validation every 12 months. drive.file + Picker avoids this entirely but grants per-file access, making it 'import from Drive' rather than folder-watching. Dropbox app-folder scope is tight and light-review. Provider burden is therefore ASYMMETRIC and this likely matters more to the decision than the engineering does.
- (constraint by coach) AD-1 amendment is a PREREQUISITE of the connector epic, not part of it. Left unamended for now because it is currently true and weakening a live invariant ahead of the work would be wrong. Recorded in Deferred so whoever picks the epic up finds it.
